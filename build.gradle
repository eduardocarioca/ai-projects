// Compatibility stub for the repo root.
// The active Flutter project is the standard structure under lib/ and android/.
// Use: flutter pub get, flutter analyze, flutter test, flutter run.

plugins {
    id 'base'
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
